# セキュリティ

メモリは永続化されるため、チャット文脈より厳しく扱う。

## 保存しないもの

- パスワード
- API トークン
- SSH 秘密鍵
- DB 接続情報
- Cookie
- `.env` の中身

## 保存してよいもの

秘密値そのものではなく、管理場所だけを書く。

```text
DEPLOY_SSH_KEY はデプロイ用シークレットマネージャーで管理する。
```

## 初期方針

- ローカル保存のみ
- クラウド同期なし
- テレメトリなし
- エクスポートは明示操作のみ

明らかな秘密情報は `remember` で拒否する。
